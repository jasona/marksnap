<Window x:Class="MarkSnap.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="MarkSnap - Markdown Viewer"
        Height="800"
        Width="1000"
        MinHeight="400"
        MinWidth="600"
        WindowStartupLocation="Manual"
        Background="#1e1e1e"
        AllowDrop="True"
        Drop="Window_Drop"
        DragOver="Window_DragOver"
        DragLeave="Window_DragLeave">

    <Window.Resources>
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3c3c3c"/>
            <Setter Property="Foreground" Value="#cccccc"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4c4c4c"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#5c5c5c"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#2d2d2d" Padding="10,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button x:Name="OpenButton" Content="Open" Click="OpenButton_Click"
                            Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,8,0"/>
                    <Button x:Name="RefreshButton" Content="Refresh" Click="RefreshButton_Click"
                            Style="{StaticResource ToolbarButtonStyle}"/>
                </StackPanel>

                <TextBlock x:Name="FileNameText" Grid.Column="1"
                           Text="No file loaded"
                           Foreground="#888888"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           FontSize="13"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="RegisterButton" Content="Set as Default" Click="RegisterButton_Click"
                            Style="{StaticResource ToolbarButtonStyle}" ToolTip="Register MarkSnap as the default .md file handler"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- WebView for Markdown rendering -->
        <wv2:WebView2 x:Name="MarkdownView"
                      Grid.Row="1"
                      DefaultBackgroundColor="#1e1e1e"/>

        <!-- Welcome panel (shown when no file is loaded) -->
        <Border x:Name="WelcomePanel" Grid.Row="1" Background="#1e1e1e" Visibility="Visible"
                AllowDrop="True" Drop="Window_Drop" DragOver="Window_DragOver">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False">
                <TextBlock Text="MarkSnap" Foreground="#ffffff" FontSize="32" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                <TextBlock Text="Markdown Viewer" Foreground="#888888" FontSize="16" HorizontalAlignment="Center" Margin="0,5,0,30"/>
                <TextBlock Text="Drop a .md file here or click Open to get started"
                           Foreground="#666666" FontSize="14" HorizontalAlignment="Center"/>
                <TextBlock Text="You can also double-click .md files after registering MarkSnap as the default handler"
                           Foreground="#555555" FontSize="12" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <!-- Invisible drop zone overlay (on top of WebView2 when content is loaded) -->
        <Border x:Name="DropZone" Grid.Row="1" Background="Transparent"
                AllowDrop="True" Drop="Window_Drop" DragOver="Window_DragOver"
                IsHitTestVisible="False"/>

        <!-- Status bar -->
        <Border Grid.Row="2" Background="#007acc" Padding="10,4">
            <TextBlock x:Name="StatusText" Text="Ready" Foreground="White" FontSize="12"/>
        </Border>
    </Grid>
</Window>
